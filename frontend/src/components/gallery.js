const Gallery=()=>{
    return (
        <div>
            <div className="">
                {/* navbar with a icon for the profile */}
            </div>
            <h1>Gallery page</h1>
            {/* show if the google account is added */}
            {/* if not, prompt */}
            {/* else show the pics */}
        </div>
    )
}

export default Gallery;